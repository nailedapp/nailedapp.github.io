baseURL = "https://nailedapp.io"
languageCode = "en-gb"
title = "Nailed — Stop Biting Your Nails | Mac App with AI Detection"
enableRobotsTXT = true

[params]
  description = "A macOS menu bar app that uses on-device machine learning to detect nail-biting and alert you in real time. Private, offline, works instantly."
  appStoreURL = "https://apps.apple.com/app/nailed-stop-biting-nails/id6761733224"
  price = "$4.99"

[sitemap]
  changefreq = "weekly"
  priority = 0.5

[outputs]
  home = ["HTML", "RSS"]
  section = ["HTML", "RSS"]

[taxonomies]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
